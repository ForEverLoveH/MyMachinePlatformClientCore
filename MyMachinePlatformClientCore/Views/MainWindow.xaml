<Window x:Class="MyMachinePlatformClientCore.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MyMachinePlatformClientCore.Views"
        xmlns:prism="http://prismlibrary.com/"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        prism:ViewModelLocator.AutoWireViewModel="True"
        Background="Transparent"
        Title="MainWindow"   >
    <Window.Resources>
        
    </Window.Resources>
    <Grid Style="{StaticResource GlobalGridStyle}">
        <Viewbox  Width="1920" Height="1080" Margin="0,0,0,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="10*"/>
                    <RowDefinition Height="200*"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="10*"/>
                        <ColumnDefinition Width="200*"/>
                    </Grid.ColumnDefinitions>
                    <Grid Grid.Row="1"   Grid.Column="1" Margin="2,2,2,2">
                        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                            
                            
                        </ScrollViewer>
                    </Grid>
            </Grid>




        </Viewbox>
    </Grid>
</Window>
